pluginManagement {
    repositories {
        mavenCentral()
        google()
        maven { url = uri("../node_modules/react-native/android") }
    }
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

rootProject.name = 'press-to-speak'
include ':app'
